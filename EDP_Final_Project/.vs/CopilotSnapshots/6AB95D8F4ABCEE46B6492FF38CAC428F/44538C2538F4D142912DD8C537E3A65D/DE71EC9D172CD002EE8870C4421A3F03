namespace ElevatorSim
{
    partial class ElevatorForm
    {
        private System.ComponentModel.IContainer components = null;

        protected override void Dispose(bool disposing)
        {
            if (disposing && (components != null))
            {
                components.Dispose();
            }
            base.Dispose(disposing);
        }

        private void InitializeComponent()
        {
            components = new System.ComponentModel.Container();
            this.Text = "Event-Driven Elevator Simulator - 3 Floors";
            this.Size = new System.Drawing.Size(1200, 800);
            this.StartPosition = FormStartPosition.CenterScreen;
            this.BackColor = System.Drawing.Color.FromArgb(240, 240, 240);

            // Main panel
            Panel mainPanel = new Panel();
            mainPanel.Dock = DockStyle.Fill;
            mainPanel.Padding = new Padding(20);
            this.Controls.Add(mainPanel);

            // Title
            Label titleLabel = new Label();
            titleLabel.Text = "🛗 Event-Driven Elevator Control System";
            titleLabel.Font = new Font("Arial", 18, FontStyle.Bold);
            titleLabel.Dock = DockStyle.Top;
            titleLabel.Height = 40;
            titleLabel.TextAlign = ContentAlignment.MiddleCenter;
            mainPanel.Controls.Add(titleLabel);

            // Horizontal container for elevator and info
            Panel horizontalContainer = new Panel();
            horizontalContainer.Dock = DockStyle.Top;
            horizontalContainer.Height = 400;
            horizontalContainer.Margin = new Padding(0, 10, 0, 10);
            mainPanel.Controls.Add(horizontalContainer);

            // Elevator shaft visualization
            ElevatorShaft = new Panel();
            ElevatorShaft.Location = new Point(50, 10);
            ElevatorShaft.Size = new Size(350, 380);
            ElevatorShaft.BorderStyle = BorderStyle.Fixed3D;
            ElevatorShaft.BackColor = System.Drawing.Color.Black;
            ElevatorShaft.Paint += ElevatorShaft_Paint;
            horizontalContainer.Controls.Add(ElevatorShaft);

            // Info panel on the right
            Panel infoPanel = new Panel();
            infoPanel.Location = new Point(420, 10);
            infoPanel.Size = new Size(330, 380);
            infoPanel.BorderStyle = BorderStyle.Fixed3D;
            infoPanel.BackColor = System.Drawing.Color.White;
            infoPanel.Padding = new Padding(15);
            horizontalContainer.Controls.Add(infoPanel);

            // Current floor display
            Label currentFloorLabel = new Label();
            currentFloorLabel.Text = "Current Floor";
            currentFloorLabel.Font = new Font("Arial", 12, FontStyle.Bold);
            currentFloorLabel.Location = new Point(15, 20);
            currentFloorLabel.Size = new Size(300, 20);
            infoPanel.Controls.Add(currentFloorLabel);

            CurrentFloorDisplay = new Label();
            CurrentFloorDisplay.Text = "1";
            CurrentFloorDisplay.Font = new Font("Arial", 48, FontStyle.Bold);
            CurrentFloorDisplay.ForeColor = System.Drawing.Color.FromArgb(0, 100, 200);
            CurrentFloorDisplay.Location = new Point(15, 50);
            CurrentFloorDisplay.Size = new Size(300, 70);
            CurrentFloorDisplay.TextAlign = ContentAlignment.MiddleCenter;
            CurrentFloorDisplay.BorderStyle = BorderStyle.Fixed3D;
            infoPanel.Controls.Add(CurrentFloorDisplay);

            // Direction display
            Label directionLabel = new Label();
            directionLabel.Text = "Direction";
            directionLabel.Font = new Font("Arial", 11, FontStyle.Bold);
            directionLabel.Location = new Point(15, 140);
            directionLabel.Size = new Size(300, 20);
            infoPanel.Controls.Add(directionLabel);

            DirectionDisplay = new Label();
            DirectionDisplay.Text = "IDLE";
            DirectionDisplay.Font = new Font("Arial", 16, FontStyle.Bold);
            DirectionDisplay.ForeColor = System.Drawing.Color.Green;
            DirectionDisplay.Location = new Point(15, 165);
            DirectionDisplay.Size = new Size(300, 40);
            DirectionDisplay.TextAlign = ContentAlignment.MiddleCenter;
            DirectionDisplay.BorderStyle = BorderStyle.Fixed3D;
            infoPanel.Controls.Add(DirectionDisplay);

            // Pending requests display
            Label pendingLabel = new Label();
            pendingLabel.Text = "Pending Requests";
            pendingLabel.Font = new Font("Arial", 11, FontStyle.Bold);
            pendingLabel.Location = new Point(15, 225);
            pendingLabel.Size = new Size(300, 20);
            infoPanel.Controls.Add(pendingLabel);

            PendingRequestsDisplay = new Label();
            PendingRequestsDisplay.Text = "None";
            PendingRequestsDisplay.Font = new Font("Arial", 11);
            PendingRequestsDisplay.Location = new Point(15, 250);
            PendingRequestsDisplay.Size = new Size(300, 100);
            PendingRequestsDisplay.BorderStyle = BorderStyle.Fixed3D;
            PendingRequestsDisplay.AutoSize = false;
            infoPanel.Controls.Add(PendingRequestsDisplay);

            // Floor buttons panel
            Panel floorButtonsPanel = new Panel();
            floorButtonsPanel.Dock = DockStyle.Top;
            floorButtonsPanel.Height = 120;
            floorButtonsPanel.Margin = new Padding(0, 10, 0, 10);
            mainPanel.Controls.Add(floorButtonsPanel);

            Label floorButtonsLabel = new Label();
            floorButtonsLabel.Text = "📞 Call Elevator to Floor:";
            floorButtonsLabel.Font = new Font("Arial", 12, FontStyle.Bold);
            floorButtonsLabel.Location = new Point(0, 10);
            floorButtonsLabel.Size = new Size(250, 25);
            floorButtonsPanel.Controls.Add(floorButtonsLabel);

            FloorButtons = new Button[3];
            string[] floorNames = { "Ground Floor (Floor 1)", "Floor 2", "Floor 3" };
            for (int i = 1; i <= 3; i++)
            {
                Button btn = new Button();
                btn.Text = floorNames[i - 1];
                btn.Location = new Point(50 + (i - 1) * 300, 50);
                btn.Size = new Size(280, 50);
                btn.Font = new Font("Arial", 12, FontStyle.Bold);
                btn.BackColor = System.Drawing.Color.LightBlue;
                btn.ForeColor = System.Drawing.Color.Black;
                btn.Tag = i;
                btn.Click += FloorButton_Click;
                btn.Cursor = Cursors.Hand;
                floorButtonsPanel.Controls.Add(btn);
                FloorButtons[i - 1] = btn;
            }

            // Status log panel
            Panel statusPanel = new Panel();
            statusPanel.Dock = DockStyle.Fill;
            mainPanel.Controls.Add(statusPanel);

            Label logLabel = new Label();
            logLabel.Text = "📋 Event Log:";
            logLabel.Font = new Font("Arial", 12, FontStyle.Bold);
            logLabel.Dock = DockStyle.Top;
            logLabel.Height = 30;
            statusPanel.Controls.Add(logLabel);

            EventLogDisplay = new ListBox();
            EventLogDisplay.Dock = DockStyle.Fill;
            EventLogDisplay.Font = new Font("Courier New", 10);
            statusPanel.Controls.Add(EventLogDisplay);
        }

        private Label? CurrentFloorDisplay;
        private Label? DirectionDisplay;
        private Label? PendingRequestsDisplay;
        private Button[]? FloorButtons;
        private ListBox? EventLogDisplay;
        private Panel? ElevatorShaft;

        public Label? GetCurrentFloorDisplay() => CurrentFloorDisplay;
        public Label? GetDirectionDisplay() => DirectionDisplay;
        public Label? GetPendingRequestsDisplay() => PendingRequestsDisplay;
        public Button[]? GetFloorButtons() => FloorButtons;
        public ListBox? GetEventLogDisplay() => EventLogDisplay;
        public Panel? GetElevatorShaft() => ElevatorShaft;

        private void FloorButton_Click(object? sender, EventArgs e)
        {
            if (sender is Button btn && btn.Tag is int floor)
            {
                // This will be handled in the form code-behind
            }
        }

        private void ElevatorShaft_Paint(object? sender, PaintEventArgs e)
        {
            e.Graphics.Clear(System.Drawing.Color.Black);
            // Drawing logic will be in ElevatorForm.cs
        }
    }
}
